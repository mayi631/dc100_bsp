From 4efde1a9eabea7c40da0283eb4a5eaf5161db5de Mon Sep 17 00:00:00 2001
From: "xiu.yang" <xiu.yang@sophgo.com>
Date: Thu, 27 Aug 2026 11:44:12 +0800
Subject: [PATCH 1/3] fix:freelist be malloc in interupt

Change-Id: I8ac30cd52c21529b287b509fdbad7b60e313a43c
---
 interdrv/vc_drv/driver/venc.c        | 111 +++++++++------------------
 interdrv/vi/driver/vi.c              |  62 ++++++++-------
 interdrv/vi/driver/vi_defines.h      |   5 ++
 interdrv/vi/driver/vi_ioctl.c        |   2 +-
 interdrv/vi/driver/vi_isp_buf_ctrl.h |   1 +
 5 files changed, 78 insertions(+), 103 deletions(-)

diff --git a/interdrv/vc_drv/driver/venc.c b/interdrv/vc_drv/driver/venc.c
index bba8dd8d..5dbb887e 100644
--- a/interdrv/vc_drv/driver/venc.c
+++ b/interdrv/vc_drv/driver/venc.c
@@ -10,7 +10,7 @@
 #include "vpss_cb.h"
 
 venc_context *g_venc_chns_ctx;
-unsigned int venc_log_lv = 1;
+unsigned int venc_log_lv = DRV_VENC_MASK_WARN;
 
 #ifndef FPGA_PORTING
 #define WAIT_SBM_TIMEOUT 2000
@@ -3848,13 +3848,6 @@ int reset_sbm(int clr_all)
 	int ret = 0;
 	int reg = 0;
 	//DRV_VENC_INFO("%s pid:%d comm:%s\n", __func__, current->pid, current->comm);
-	DRV_VENC_INFO("Before sw reset sb =================\n");
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x00 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x00));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x80 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x80));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x84 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x84));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x88 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x88));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x90 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x90));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x94 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x94));
 
 	// Reset VC SB ctrl
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x00);
@@ -3865,13 +3858,6 @@ int reset_sbm(int clr_all)
 		vc_lite_mmu_write_reg(REG_SBM, 0x00, 0);
 
 
-	DRV_VENC_INFO("After sw reset sb =================\n");
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x00 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x00));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x80 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x80));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x84 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x84));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x88 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x88));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x90 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x90));
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x94 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x94));
 
 	return ret;
 }
@@ -3954,7 +3940,9 @@ int drv_venc_destroy_chn(venc_chn ve_chn)
 
 	if (p_chn_handle->sbm_state != VENC_SBM_STATE_IDLE
 		&& p_sbm_handle->p_sbm_enc_thread) {
+		osal_printk_auto("[destroy_chn %d] sbm_mutex lock +\n", ve_chn);
 		osal_mutex_lock(&p_sbm_handle->sbm_mutex);
+		osal_printk_auto("[destroy_chn %d] sbm_mutex lock -\n", ve_chn);
 		if (p_chn_handle->p_chn_attr->st_venc_attr.en_type == PT_JPEG)
 			p_sbm_handle->jpeg_en = 0;
 		p_chn_handle->sbm_state = VENC_SBM_STATE_CHN_CLOSED;
@@ -3963,7 +3951,9 @@ int drv_venc_destroy_chn(venc_chn ve_chn)
 		p_sbm_handle->sbm_num--;
 	}
 
+	osal_printk_auto("[destroy_chn %d] enc close +\n", ve_chn);
 	ret = p_enc_ctx->base.close(p_enc_ctx);
+	osal_printk_auto("[destroy_chn %d] enc close - ret=%d\n", ve_chn, ret);
 
 	if (p_chn_handle->p_chn_vars) {
 		osal_vfree(p_chn_handle->p_chn_vars);
@@ -3995,8 +3985,9 @@ int drv_venc_destroy_chn(venc_chn ve_chn)
 		if (p_sbm_handle->p_sbm_enc_thread && b_free_sbm_thread) {
 			p_sbm_handle->sbm_exit = 1;
 			wake_sbm_waitting();
-			DRV_VENC_INFO("sbm thread exit\n");
+			osal_printk_auto("[destroy_chn %d] kthread_destroy +\n", ve_chn);
 			osal_kthread_destroy(p_sbm_handle->p_sbm_enc_thread, 1);
+			osal_printk_auto("[destroy_chn %d] kthread_destroy -\n", ve_chn);
 			p_sbm_handle->p_sbm_enc_thread = NULL;
 			osal_mutex_destroy(&p_sbm_handle->sbm_mutex);
 			reset_sbm(1);
@@ -4007,7 +3998,7 @@ int drv_venc_destroy_chn(venc_chn ve_chn)
 				osal_free(p_sbm_handle->sbm_info);
 				p_sbm_handle->sbm_info = NULL;
 			}
-			DRV_VENC_INFO("sbm thread exit ok\n");
+			osal_printk_auto("[destroy_chn %d] sbm thread exit ok\n", ve_chn);
 		}
 		if (p_sbm_handle->sbm_clk_en && !p_sbm_handle->p_sbm_enc_thread) {
 			sbm_combo_clk_disable();
@@ -4054,8 +4045,10 @@ int drv_venc_reset_chn(venc_chn ve_chn)
 		venc_enc_ctx *p_enc_ctx = &p_chn_handle->enc_ctx;
 
 		if (p_enc_ctx->base.ioctl) {
+			osal_printk_auto("[reset_chn %d] jpeg ioctl reset +\n", ve_chn);
 			ret = p_enc_ctx->base.ioctl(
 				p_enc_ctx, DRV_JPEG_OP_RESET_CHN, NULL);
+			osal_printk_auto("[reset_chn %d] jpeg ioctl reset - ret=%d\n", ve_chn, ret);
 			if (ret != 0) {
 				DRV_VENC_ERR(
 					"DRV_JPEG_OP_RESET_CHN fail, ret = %d\n",
@@ -6158,6 +6151,7 @@ static int venc_sbm_send_frame_thread(void *data)
 	osal_timeval ts;
 	int next_frm_slice_ready = 0;
 	int push_cnt = 0;
+	int exit_handled = 0;
 
 	vc_sbm_irq_enable(SBM_IRQ_PUSH_BIT);
 	DRV_VENC_INFO("smb thread start 0x94=0x%x 0x90=0x%x\n",
@@ -6165,7 +6159,11 @@ static int venc_sbm_send_frame_thread(void *data)
 		vc_lite_mmu_read_reg(REG_SBM, 0x90));
 	(void) data;
 	p_sbm_handle = get_sbm_ctx();
-	while (!osal_kthread_should_stop()) {
+	while (1) {
+		if (osal_kthread_should_stop()) {
+			exit_handled = 1;
+			break;
+		}
 		if (!next_frm_slice_ready) {
 			if (sbm_wait_interrupt(WAIT_SBM_TIMEOUT) == -1) {
 				if (!p_sbm_handle->jpeg_en) {
@@ -6179,7 +6177,11 @@ static int venc_sbm_send_frame_thread(void *data)
 		} else {
 			next_frm_slice_ready = 0;
 		}
-		if (osal_kthread_should_stop() || g_venc_chns_ctx == NULL)
+		if (osal_kthread_should_stop()) {
+			exit_handled = 1;
+			break;
+		}
+		if (g_venc_chns_ctx == NULL)
 			break;
 
 		if (p_sbm_handle->sbm_exit) {
@@ -6278,7 +6280,8 @@ static int venc_sbm_send_frame_thread(void *data)
 			vc_lite_mmu_read_reg(REG_SBM, 0x90), next_frm_slice_ready);
 	}
 	DRV_VENC_LOG("sbm_send_frame_thread exit\n");
-	osal_kthread_exit();
+	if (!exit_handled)
+		osal_kthread_exit();
 	return ret;
 }
 
@@ -6475,11 +6478,10 @@ int sbm_skip_overflow_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_sbm_se
 	int s32_count = 0;
 	int max_count = 0;
 	unsigned int u32_reg94;
-	unsigned int u32_reg90;
-	unsigned int u32_reg80;
+	// unsigned int u32_reg90;
+	// unsigned int u32_reg80;
 
 
-	DRV_VENC_DBG("ve_chn = %d\n", ve_chn);
 	ret = _check_venc_chn_handle(ve_chn);
 	if (ret != 0) {
 		DRV_VENC_ERR("check_chn_handle, %d\n", ret);
@@ -6494,14 +6496,12 @@ int sbm_skip_overflow_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_sbm_se
 	p_chn_handle->b_sb_skip_frm = 1;
 
 	// store before skip
-	u32_reg80 = vc_lite_mmu_read_reg(REG_SBM, 0x80);
-	u32_reg90 = vc_lite_mmu_read_reg(REG_SBM, 0x90);
+	// u32_reg80 = vc_lite_mmu_read_reg(REG_SBM, 0x80);
+	// u32_reg90 = vc_lite_mmu_read_reg(REG_SBM, 0x90);
 	u32_reg94 = vc_lite_mmu_read_reg(REG_SBM, 0x94);
 
 	// pri push cnt is 0 just return
 	if (!(u32_reg94 & 0x3F)) {
-		DRV_VENC_DBG("Chn:%d NO slice pushed, just return,80:0x%x, 90:0x%x, 94:0x%x\n",
-			ve_chn, u32_reg80, u32_reg90, u32_reg94);
 		ret = 0;
 		goto SKIP_FRAME_END;
 	}
@@ -6520,15 +6520,7 @@ int sbm_skip_overflow_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_sbm_se
 		ret = venc_sbm_trig_dummy_push(pst_sbm_setting);
 		ret = venc_sbm_get_skip_frm_status(pst_sbm_setting);
 		if (ret == 1) {
-			ret = 0;
-			DRV_VENC_INFO("chn:%d pushcnt:%d maxcnt:%d\n", ve_chn, s32_count, max_count);
-			DRV_VENC_INFO("SkipFrm done old 80:0x%x, 90:0x%x, 94:0x%x; new 80:0x%x, 90:0x%x, 94:0x%x\n",
-				u32_reg80,
-				u32_reg90,
-				u32_reg94,
-				vc_lite_mmu_read_reg(REG_SBM, 0x80),
-				vc_lite_mmu_read_reg(REG_SBM, 0x90),
-				vc_lite_mmu_read_reg(REG_SBM, 0x94));
+				ret = 0;
 			break;
 		}
 		osal_mdelay(100);
@@ -6585,7 +6577,6 @@ int drv_venc_cb_snap_jpg_frame(int venc_chn, unsigned int frm_cnt)
 	}
 
 	p_chn_handle->jpg_frm_skip_cnt = frm_cnt;
-	DRV_VENC_INFO("chn(%d), jpg_frm_skip_cnt = %d\n", venc_chn, p_chn_handle->jpg_frm_skip_cnt);
 
 	ret = 0;
 
@@ -6626,7 +6617,6 @@ int drv_venc_cb_switch_chn(int venc_chn, u8 snr_num, u64 frm_num)
 		reset_sbm(1);
 		if (p_sbm_handle->mismatch_reset)
 			reset_wake_sbm_waitting();
-		DRV_VENC_DBG("sbm reset in switch chn\n");
 		p_sbm_handle->need_reset = 0;
 		p_sbm_handle->mismatch_reset = 0;
 		vc_set_sbm_reg(&p_chn_handle->st_sbm_setting, 0);
@@ -6634,10 +6624,6 @@ int drv_venc_cb_switch_chn(int venc_chn, u8 snr_num, u64 frm_num)
 	p_sbm_handle->curr_sbm_chn = venc_chn;
 	p_sbm_handle->st_sbm_info.snr_num = snr_num;
 	p_sbm_handle->st_sbm_info.frm_num = frm_num;
-	DRV_VENC_DBG("chn(%d), snr_num = %d, frm_num = %lld y_pos = %d, uv_pos = %d\n",
-		p_chn_handle->ve_chn, p_sbm_handle->st_sbm_info.snr_num,
-		p_sbm_handle->st_sbm_info.frm_num,
-		p_sbm_handle->y_pos, p_sbm_handle->uv_pos);
 	return 0;
 }
 
@@ -6664,7 +6650,6 @@ static int venc_sbm_en_dummy_pop(struct venc_sbm_setting_s *pst_sbm_setting)
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x14);
 	reg |= 0x10;  // reg_pri_pop_ow_en bit4
 	vc_lite_mmu_write_reg(REG_SBM, 0x14, reg);
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x14 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x14));
 
 	return ret;
 }
@@ -6674,32 +6659,24 @@ static int venc_sbm_trig_dummy_pop(struct venc_sbm_setting_s *pst_sbm_setting)
 	int ret = 0;
 	int reg = 0;
 	int pop_cnt_pri = 0;
-	int push_cnt_pri = 0;
+	// int push_cnt_pri = 0;
 	int target_slice_cnt = 0;
 
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x94);
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x94 = 0x%x\n", reg);
 
-	push_cnt_pri = reg & 0x3F;
+	// push_cnt_pri = reg & 0x3F;
 	pop_cnt_pri = (reg >> 16) & 0x3F;
-	DRV_VENC_INFO("push_cnt_pri=%d, pop_cnt_pri=%d\n", push_cnt_pri, pop_cnt_pri);
 	if (pst_sbm_setting->sb_size == 0)
 		target_slice_cnt = (pst_sbm_setting->src_height + 63) / 64;
 	else
 		target_slice_cnt = (pst_sbm_setting->src_height + 127) / 128;
 
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x90);
-	DRV_VENC_INFO("0x80 = 0x%x 0x88 = 0x%x 0x90 = 0x%x 0x94 = 0x%x",
-	vc_lite_mmu_read_reg(REG_SBM, 0x80),
-	vc_lite_mmu_read_reg(REG_SBM, 0x88),
-	vc_lite_mmu_read_reg(REG_SBM, 0x90),
-	vc_lite_mmu_read_reg(REG_SBM, 0x94));
 
 	if ((pop_cnt_pri <= target_slice_cnt) && reg) {
 		reg = vc_lite_mmu_read_reg(REG_SBM, 0x14);
 		reg |= 0x40;  // reg_pri_push_ow bit6
 		vc_lite_mmu_write_reg(REG_SBM, 0x14, reg);
-		DRV_VENC_INFO("VC_REG_BANK_SBM 0x94 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x94));
 	}
 
 	return ret;
@@ -6712,7 +6689,6 @@ static int venc_sbm_dis_dummy_pop(struct venc_sbm_setting_s *pst_sbm_setting)
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x14);
 	reg &= (~0x10);  // reg_pri_push_ow_en bit0
 	vc_lite_mmu_write_reg(REG_SBM, 0x14, reg);
-	DRV_VENC_INFO("VC_REG_BANK_SBM 0x14 = 0x%x\n", vc_lite_mmu_read_reg(REG_SBM, 0x14));
 
 	return ret;
 }
@@ -6725,11 +6701,10 @@ int sbm_venc_side_drop_one_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_s
 	int max_count = 0;
 	int reg = 0;
 	unsigned int u32_reg94;
-	unsigned int u32_reg90;
-	unsigned int u32_reg80;
+	// unsigned int u32_reg90;
+	// unsigned int u32_reg80;
 	int used_hw_drop = 1;
 
-	DRV_VENC_DBG("ve_chn = %d\n", ve_chn);
 	reg = vc_lite_mmu_read_reg(REG_SBM, 0x00);
 
 	// bit7  sbc0 h265
@@ -6750,7 +6725,6 @@ int sbm_venc_side_drop_one_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_s
 
 	vc_lite_mmu_write_reg(REG_SBM, 0x00, reg);
 	if (used_hw_drop) {
-		DRV_VENC_INFO("used hw drop in\n");
 		if (sbm_wait_interrupt(2000) == -1) {
 			DRV_VENC_ERR("auto skip fail\n");
 		}
@@ -6764,11 +6738,6 @@ int sbm_venc_side_drop_one_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_s
 			vc_sbm_irq_disable(SBM_IRQ_FRAME_DOEN_BIT);
 
 		vc_sbm_irq_enable(SBM_IRQ_PUSH_BIT);
-		DRV_VENC_INFO("drop frame done 80:0x%x, 88 0x%x 90:0x%x, 94:0x%x\n",
-				vc_lite_mmu_read_reg(REG_SBM, 0x80),
-				vc_lite_mmu_read_reg(REG_SBM, 0x88),
-				vc_lite_mmu_read_reg(REG_SBM, 0x90),
-				vc_lite_mmu_read_reg(REG_SBM, 0x94));
 		return 0;
 	}
 	ret = _check_venc_chn_handle(ve_chn);
@@ -6784,14 +6753,12 @@ int sbm_venc_side_drop_one_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_s
 	//p_chn_handle->b_sb_skip_frm = 1;
 
 	// store before skip
-	u32_reg80 = vc_lite_mmu_read_reg(REG_SBM, 0x80);
-	u32_reg90 = vc_lite_mmu_read_reg(REG_SBM, 0x90);
+	// u32_reg80 = vc_lite_mmu_read_reg(REG_SBM, 0x80);
+	// u32_reg90 = vc_lite_mmu_read_reg(REG_SBM, 0x90);
 	u32_reg94 = vc_lite_mmu_read_reg(REG_SBM, 0x94);
 
 	// pri push cnt is 0 just return
 	if (!(u32_reg94 & 0x3F)) {
-		DRV_VENC_DBG("Chn:%d NO slice pushed, just return,80:0x%x, 90:0x%x, 94:0x%x\n",
-			ve_chn, u32_reg80, u32_reg90, u32_reg94);
 		ret = 0;
 		goto SKIP_FRAME_END;
 	}
@@ -6808,15 +6775,7 @@ int sbm_venc_side_drop_one_frm(venc_chn ve_chn, struct venc_sbm_setting_s *pst_s
 		ret = venc_sbm_trig_dummy_pop(pst_sbm_setting);
 		ret = venc_sbm_get_skip_frm_status(pst_sbm_setting);
 		if (ret == 1) {
-			ret = 0;
-			DRV_VENC_INFO("chn:%d pushcnt:%d maxcnt:%d\n", ve_chn, s32_count, max_count);
-			DRV_VENC_INFO("SkipFrm done old 80:0x%x, 90:0x%x, 94:0x%x; new 80:0x%x, 90:0x%x, 94:0x%x\n",
-				u32_reg80,
-				u32_reg90,
-				u32_reg94,
-				vc_lite_mmu_read_reg(REG_SBM, 0x80),
-				vc_lite_mmu_read_reg(REG_SBM, 0x90),
-				vc_lite_mmu_read_reg(REG_SBM, 0x94));
+				ret = 0;
 			break;
 		}
 		osal_mdelay(1);
diff --git a/interdrv/vi/driver/vi.c b/interdrv/vi/driver/vi.c
index 4c7fb770..a5db1bc6 100755
--- a/interdrv/vi/driver/vi.c
+++ b/interdrv/vi/driver/vi.c
@@ -755,14 +755,10 @@ static inline void _vi_wake_up_preraw_th(struct vi_dev *vdev, const enum sop_isp
 
 	osal_spin_lock_irqsave(&raw_num_q->lock, &flags);
 
-	if (raw_num_q->count < VI_MAX_LIST_NUM) {
-		n = osal_kzalloc(sizeof(*n), OSAL_GFP_ATOMIC);
-		if (n) {
-			raw_num_q->count++;
-		} else {
-			vi_pr(VI_ERR, "pre_raw_num_q osal_kzalloc size(%zu) fail\n", sizeof(*n));
-			goto unlock;
-		}
+	if (!osal_list_empty(&raw_num_q->free_list)) {
+		n = osal_list_first_entry(&raw_num_q->free_list, struct _isp_raw_num_n, list);
+		osal_list_del_init(&n->list);
+		raw_num_q->count++;
 	} else if (!osal_list_empty(&raw_num_q->list)) {
 		n = osal_list_first_entry(&raw_num_q->list, struct _isp_raw_num_n, list);
 		osal_list_del_init(&n->list);
@@ -801,14 +797,10 @@ void vi_event_queue(struct vi_dev *vdev, const u32 type, const u32 frm_num)
 
 	osal_spin_lock_irqsave(&event_q->lock, &flags);
 
-	if (event_q->count < VI_MAX_LIST_NUM) {
-		ev_k = osal_kzalloc(sizeof(*ev_k), OSAL_GFP_ATOMIC);
-		if (ev_k) {
-			event_q->count++;
-		} else {
-			vi_pr(VI_ERR, "event queue osal_kzalloc size(%zu) fail\n", sizeof(*ev_k));
-			goto unlock;
-		}
+	if (!osal_list_empty(&event_q->free_list)) {
+		ev_k = osal_list_first_entry(&event_q->free_list, struct vi_event_k, list);
+		osal_list_del_init(&ev_k->list);
+		event_q->count++;
 	} else if (!osal_list_empty(&event_q->list)) {
 		ev_k = osal_list_first_entry(&event_q->list, struct vi_event_k, list);
 		osal_list_del_init(&ev_k->list);
@@ -1017,9 +1009,16 @@ void sop_isp_dqbuf_list(struct vi_dev *vdev, const u32 frm_num,
 	struct _isp_dqbuf_n *n;
 	struct isp_buf_q *dqbuf_q = &vdev->dqbuf_q;
 
-	n = osal_kzalloc(sizeof(struct _isp_dqbuf_n), OSAL_GFP_ATOMIC);
-	if (n == NULL) {
-		vi_pr(VI_ERR, "DQbuf kmalloc size(%zu) fail\n", sizeof(struct _isp_dqbuf_n));
+	osal_spin_lock_irqsave(&dqbuf_q->lock, &flags);
+	if (!osal_list_empty(&dqbuf_q->free_list)) {
+		n = osal_list_first_entry(&dqbuf_q->free_list, struct _isp_dqbuf_n, list);
+		osal_list_del_init(&n->list);
+	} else if (!osal_list_empty(&dqbuf_q->list)) {
+		n = osal_list_first_entry(&dqbuf_q->list, struct _isp_dqbuf_n, list);
+		osal_list_del_init(&n->list);
+	} else {
+		osal_spin_unlock_irqrestore(&dqbuf_q->lock, &flags);
+		vi_pr(VI_ERR, "DQbuf pool exhausted\n");
 		return;
 	}
 	n->pipe_id	= pipe_id;
@@ -1027,7 +1026,6 @@ void sop_isp_dqbuf_list(struct vi_dev *vdev, const u32 frm_num,
 	n->frm_num	= frm_num;
 	n->tv		= tv;
 
-	osal_spin_lock_irqsave(&dqbuf_q->lock, &flags);
 	osal_list_add_tail(&n->list, &dqbuf_q->list);
 	osal_spin_unlock_irqrestore(&dqbuf_q->lock, &flags);
 }
@@ -1046,7 +1044,7 @@ int vi_dqbuf(struct isp_buf_q *dqbuf_q, struct _vi_buffer *b)
 		b->sequence		= n->frm_num;
 		b->tv			= n->tv;
 		osal_list_del_init(&n->list);
-		osal_kfree(n);
+		osal_list_add_tail(&n->list, &dqbuf_q->free_list);
 		ret = 0;
 	}
 	osal_spin_unlock_irqrestore(&dqbuf_q->lock, &flags);
@@ -3437,7 +3435,7 @@ static int _vi_preraw_thread(void *arg)
 			osal_spin_lock_irqsave(&raw_num_q->lock, &flags);
 			osal_list_del_init(&n[i]->list);
 			--raw_num_q->count;
-			osal_kfree(n[i]);
+			osal_list_add_tail(&n[i]->list, &raw_num_q->free_list);
 			osal_spin_unlock_irqrestore(&raw_num_q->lock, &flags);
 
 			pipe = ctx->isp_csi_cfg[raw_num].bind_pipe[0];
@@ -4834,14 +4832,14 @@ static int clean_misc_resources(struct vi_dev *vdev)
 	osal_spin_lock_irqsave(&dqbuf_q->lock, &flags);
 	osal_list_for_each_entry_safe(n, dq_tmp, &dqbuf_q->list, list) {
 		osal_list_del_init(&n->list);
-		osal_kfree(n);
+		osal_list_add_tail(&n->list, &dqbuf_q->free_list);
 	}
 	osal_spin_unlock_irqrestore(&dqbuf_q->lock, &flags);
 
 	osal_spin_lock_irqsave(&event_q->lock, &flags);
 	osal_list_for_each_entry_safe(ev_k, ev_tmp, &event_q->list, list) {
 		osal_list_del_init(&ev_k->list);
-		osal_kfree(ev_k);
+		osal_list_add_tail(&ev_k->list, &event_q->free_list);
 	}
 	event_q->count = 0;
 	osal_spin_unlock_irqrestore(&event_q->lock, &flags);
@@ -4849,7 +4847,7 @@ static int clean_misc_resources(struct vi_dev *vdev)
 	osal_spin_lock_irqsave(&raw_num_q->lock, &flags);
 	osal_list_for_each_entry_safe(raw_n, raw_tmp, &raw_num_q->list, list) {
 		osal_list_del_init(&raw_n->list);
-		osal_kfree(raw_n);
+		osal_list_add_tail(&raw_n->list, &raw_num_q->free_list);
 	}
 	raw_num_q->count = 0;
 	osal_spin_unlock_irqrestore(&raw_num_q->lock, &flags);
@@ -5079,7 +5077,7 @@ int vi_csi_stop_streaming(struct vi_dev *vdev, uint8_t raw_num)
 			if (raw_n->raw_num == raw_num) {
 				osal_list_del_init(&raw_n->list);
 				--raw_num_q->count;
-				osal_kfree(raw_n);
+				osal_list_add_tail(&raw_n->list, &raw_num_q->free_list);
 			}
 		}
 		osal_spin_unlock_irqrestore(&raw_num_q->lock, &flags);
@@ -5445,11 +5443,23 @@ static void _vi_init_param(struct vi_dev *vdev)
 	}
 
 	OSAL_INIT_LIST_HEAD(&vdev->pre_raw_num_q.list);
+	OSAL_INIT_LIST_HEAD(&vdev->pre_raw_num_q.free_list);
 	OSAL_INIT_LIST_HEAD(&vdev->dqbuf_q.list);
+	OSAL_INIT_LIST_HEAD(&vdev->dqbuf_q.free_list);
 	OSAL_INIT_LIST_HEAD(&vdev->event_q.list);
+	OSAL_INIT_LIST_HEAD(&vdev->event_q.free_list);
 	vdev->pre_raw_num_q.count = 0;
 	vdev->event_q.count = 0;
 
+	{
+		int _i;
+		for (_i = 0; _i < VI_MAX_LIST_NUM; _i++) {
+			osal_list_add_tail(&vdev->raw_num_pool[_i].list, &vdev->pre_raw_num_q.free_list);
+			osal_list_add_tail(&vdev->event_pool[_i].list, &vdev->event_q.free_list);
+			osal_list_add_tail(&vdev->dqbuf_pool[_i].list, &vdev->dqbuf_q.free_list);
+		}
+	}
+
 	osal_spin_lock_init(&vdev->pre_raw_num_q.lock);
 	osal_spin_lock_init(&vdev->dqbuf_q.lock);
 	osal_spin_lock_init(&vdev->event_q.lock);
diff --git a/interdrv/vi/driver/vi_defines.h b/interdrv/vi/driver/vi_defines.h
index c72493c6..f0d1f3f3 100644
--- a/interdrv/vi/driver/vi_defines.h
+++ b/interdrv/vi/driver/vi_defines.h
@@ -59,6 +59,7 @@ struct vi_event_k {
 
 struct isp_event_q {
 	struct osal_list_head	list;
+	struct osal_list_head	free_list;
 	osal_spinlock		lock;
 	u8			count;
 };
@@ -103,6 +104,7 @@ struct _isp_raw_num_n {
 
 struct isp_sof_raw_num_q {
 	struct osal_list_head	list;
+	struct osal_list_head	free_list;
 	osal_spinlock		lock;
 	u8			count;
 };
@@ -184,7 +186,9 @@ struct vi_dev {
 
 	struct isp_buf_q		qbuf_q[VI_MAX_PIPE_NUM][VI_MAX_CHN_NUM];
 	struct isp_buf_q		dqbuf_q;
+	struct _isp_dqbuf_n		dqbuf_pool[0x20];
 	struct isp_sof_raw_num_q	pre_raw_num_q;
+	struct _isp_raw_num_n		raw_num_pool[0x20];
 
 	vb_blk				(*vi_dqbuf)(mmf_chn_s mmf_chn, void *data);
 	int				(*vi_qbuf)(mmf_chn_s mmf_chn, void *data);
@@ -199,6 +203,7 @@ struct vi_dev {
 	struct gdc_mesh			mesh[VI_MAX_PIPE_NUM][VI_MAX_CHN_NUM];
 
 	struct isp_event_q		event_q;
+	struct vi_event_k		event_pool[0x20];
 
 	struct stream_state_s		stream;
 
diff --git a/interdrv/vi/driver/vi_ioctl.c b/interdrv/vi/driver/vi_ioctl.c
index e5c2959d..b0ff4106 100644
--- a/interdrv/vi/driver/vi_ioctl.c
+++ b/interdrv/vi/driver/vi_ioctl.c
@@ -422,7 +422,7 @@ static long _vi_g_ctrl(struct vi_dev *vdev, struct vi_ctrl *ctrl)
 			ev_u->frame_sequence	= ev_k->ev.frame_sequence;
 			ev_u->pts		= ev_k->ev.pts;
 			osal_list_del_init(&ev_k->list);
-			osal_kfree(ev_k);
+			osal_list_add_tail(&ev_k->list, &event_q->free_list);
 			event_q->count--;
 		}
 		osal_spin_unlock_irqrestore(&event_q->lock, &flags);
diff --git a/interdrv/vi/driver/vi_isp_buf_ctrl.h b/interdrv/vi/driver/vi_isp_buf_ctrl.h
index da86f437..4da8243e 100644
--- a/interdrv/vi/driver/vi_isp_buf_ctrl.h
+++ b/interdrv/vi/driver/vi_isp_buf_ctrl.h
@@ -65,6 +65,7 @@ struct sop_isp_buf {
 
 struct isp_buf_q {
 	struct osal_list_head	list;
+	struct osal_list_head	free_list;
 	u8			num;
 	osal_spinlock		lock;
 };
-- 
2.25.1

